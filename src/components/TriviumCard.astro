---
import ProjectCard from './ProjectCard.astro';
import type { Locale } from '../i18n';
interface Props { locale?: Locale }
const { locale = 'zh-cn' } = Astro.props;
---
<ProjectCard locale={locale} project="trivium" />
